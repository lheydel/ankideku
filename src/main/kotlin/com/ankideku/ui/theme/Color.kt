package com.ankideku.ui.theme

import androidx.compose.ui.graphics.Color

object AppColors {
    // Primary (Green)
    val Primary = Color(0xFF0F8A35)
    val PrimaryVariant = Color(0xFF0a6b28)
    val PrimaryContainer = Color(0xFF1a5c2e)
    val OnPrimary = Color.White
    val OnPrimaryContainer = Color(0xFFb8f5c8)

    // Background
    val BackgroundLight = Color(0xFFF5F5F5)
    val BackgroundDark = Color(0xFF121212)
    val SurfaceLight = Color.White
    val SurfaceDark = Color(0xFF1E1E1E)
    val SurfaceVariantLight = Color(0xFFF0F0F0)
    val SurfaceVariantDark = Color(0xFF2D2D2D)

    // Text
    val OnBackgroundLight = Color(0xFF1A1A1A)
    val OnBackgroundDark = Color(0xFFE0E0E0)
    val OnSurfaceLight = Color(0xFF1A1A1A)
    val OnSurfaceDark = Color(0xFFE0E0E0)
    val OnSurfaceVariant = Color(0xFF757575)

    // Semantic
    val Success = Color(0xFF4CAF50)
    val Warning = Color(0xFFFFA726)
    val Error = Color(0xFFEF5350)
    val Info = Color(0xFF42A5F5)

    // Diff
    val DiffAddedBackground = Color(0xFFDCF5DC)
    val DiffAddedBackgroundDark = Color(0xFF1a3d1a)
    val DiffRemovedBackground = Color(0xFFFDECEC)
    val DiffRemovedBackgroundDark = Color(0xFF3d1a1a)
}
